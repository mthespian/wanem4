These files go into the default directory for the root user on the wanem4 box.
(e.g. /root )

Note, if their directory is not /root, the paths added to sudoers must change to reflect their new location.
